import React from 'react';
import { HeaderProps } from './types';
declare function Header<T>({ className, readOnly, selectedRange, onInsertRow, onDeleteRows, }: HeaderProps<T>): React.ReactElement;
export default Header;
