import React from 'react';
import { ColumnHeaderProps } from './types';
declare function ColumnHeader<T>({ className, column, sort, filter, }: ColumnHeaderProps<T>): React.ReactElement;
export default ColumnHeader;
